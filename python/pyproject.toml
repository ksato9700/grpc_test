[tool.poetry]
name = "greeter"
version = "0.1.2"
description = ""
authors = ["Ken Sato <ksato9700@gmail.com>"]

[tool.poetry.dependencies]
python = "~3.11"
grpcio = [
    {file = "packages/grpcio-1.50.0-cp311-cp311-macosx_11_0_arm64.whl", markers = "platform_system == 'Darwin' and platform_machine == 'arm64'"},
    {version = "^1.50.0", markers = "platform_system != 'Darwin' or platform_machine != 'arm64'"},
]
protobuf = "^4.21.9"

[tool.poetry.group.dev.dependencies]
black = "^22.10.0"
flake8 = "^5.0.4"
grpcio-tools = "^1.50.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
